<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div id="explanation">
      <i style="font-size: 10px; color:#FF7F7F"><%= pluralize(user.errors.count, 'error') %> prohibited this micropost from being saved</i>

      <div class="errors" style="margin-bottom:5px; margin-top:5px">
        <%= render partial: 'error', collection: user.errors%>
      </div>
    </div>
  <% end %>

  <div class="field" style="margin-bottom: 5px">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field" style="margin-bottom: 5px">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="submit">
    <%= form.submit %>
  </div>
<% end %>